# ServiceMonitor for clickstream-consumer: Prometheus scrapes metrics from the metrics sidecar (port 9090).
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: clickstream-consumer
  namespace: default
  labels:
    app: clickstream-consumer
spec:
  selector:
    matchLabels:
      app: clickstream-consumer
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
    scrapeTimeout: 10s
