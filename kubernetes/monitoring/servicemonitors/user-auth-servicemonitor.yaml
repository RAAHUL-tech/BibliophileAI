apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-auth-service
  namespace: default
  labels:
    app: user-auth
spec:
  selector:
    matchLabels:
      app: user-auth
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
    scrapeTimeout: 10s
